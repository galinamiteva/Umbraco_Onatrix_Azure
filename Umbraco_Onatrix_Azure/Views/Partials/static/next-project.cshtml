@using Umbraco.Cms.Web.Common.PublishedModels;
@inherits Umbraco.Cms.Web.Common.Views.UmbracoViewPage
@inject Umbraco.Cms.Web.Common.UmbracoHelper UmbracoHelper

@{
    var rootContents = UmbracoHelper.ContentAtRoot();
    var settings = rootContents?.FirstOrDefault(x => x.ContentType.Alias == "settingsPage") ?? null;
    var nextProjectTextHeadline = settings?.Value<string>("nextProjectTextHeadline") ?? null;
    var officeLocation = settings?.Value<string>("officeLocation") ?? null;
    var officePhoneNumber = settings?.Value<string>("officePhoneNumber") ?? null;
    var officeEmailAddress = settings?.Value<string>("officeEmailAddress") ?? null;
    var nextProjectTextLocation = settings?.Value<string>("nextProjectTextLocation") ?? null;
    var nextProjectTextPhone = settings?.Value<string>("nextProjectTextPhone") ?? null;
    var nextProjectTextEmail = settings?.Value<string>("nextProjectTextEmail") ?? null;
    var nextProjectImgHuman = settings?.Value<IPublishedContent>("nextProjectImgHuman")?.Url() ?? null;
    var nextProjectImgBackground = settings?.Value<IPublishedContent>("nextProjectImgBackground")?.Url() ?? null;
    var nextProjectImgDot = settings?.Value<IPublishedContent>("nextProjectImgDot")?.Url() ?? null;
    
  var currentPageUrl = Model.Url();
    var isContactPage = currentPageUrl.Contains("/contact");
}

<section id="next-project" class="@(isContactPage ? null : "bg-gray")">
    <div class="container">

        <div class="images">
            <div class="image-human">
                <img src="@nextProjectImgHuman" alt="">
            </div>


            <div class="image-dot">
                <img src="@nextProjectImgDot" alt="">
            </div>
        </div>

        <div class="text">

                      
                <h2>@nextProjectTextHeadline</h2>
            

            

            <h5><i class="fas fa-map-marker-alt"></i>Office location</h5>
            <p>@officeLocation</p>
            
            
            <h5><i class="fas fa-phone"></i>Phone number</h5>           
            <p>@officePhoneNumber</p>
            
            <h5><i class="fas fa-envelope"></i>E-mail address</h5>
            <p>@officeEmailAddress</p>
        </div>
    </div>
</section>